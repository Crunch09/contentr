<%= simple_form_for_contentr_paragraph(paragraph, compilation, area_name) do |f| %>

  <% paragraph.fields_for_simple_form.each do |name, opts| %>
    <%= f.input(name, opts) %>
  <% end %>

  <%= f.submit %>
<% end %>
